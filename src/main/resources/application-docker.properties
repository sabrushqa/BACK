spring.datasource.url=${APP_DATASOURCE_URL:jdbc:sqlserver://sqlserver:1433;databaseName=master5;encrypt=true;trustServerCertificate=true}
spring.datasource.username=${APP_DATASOURCE_USERNAME:sa}
spring.datasource.password=${APP_DATASOURCE_PASSWORD:}
app.affiliation.upload-dir=${APP_AFFILIATION_UPLOAD_DIR:/app/uploads/affiliations}
app.affiliation.contract-dir=${APP_AFFILIATION_CONTRACT_DIR:/app/uploads/contracts}
app.affiliation.chrome-pdf.enabled=${APP_AFFILIATION_CHROME_PDF_ENABLED:false}
app.frontend.base-url=${APP_FRONTEND_BASE_URL:http://localhost:4200}
spring.security.oauth2.resourceserver.jwt.issuer-uri=${KEYCLOAK_ISSUER_URI:http://keycloak:8080/realms/PFE26}
app.keycloak.server-url=${KEYCLOAK_SERVER_URL:http://keycloak:8080}
app.keycloak.client-id=${KEYCLOAK_CLIENT_ID:portail-affiliation}
app.keycloak.otp.required=${KEYCLOAK_OTP_REQUIRED:true}
spring.mail.host=${SPRING_MAIL_HOST:smtp.gmail.com}
spring.mail.port=${SPRING_MAIL_PORT:587}
spring.mail.username=${SPRING_MAIL_USERNAME:}
spring.mail.password=${SPRING_MAIL_PASSWORD:}
# Adresse expediteur transmise au realm Keycloak (sinon repli sur spring.mail.username)
app.keycloak.smtp.from=${APP_KEYCLOAK_SMTP_FROM:}
app.keycloak.smtp.from-display-name=${APP_KEYCLOAK_SMTP_FROM_DISPLAY_NAME:Lana Cash}
app.keycloak.smtp.reply-to=${APP_KEYCLOAK_SMTP_REPLY_TO:}
